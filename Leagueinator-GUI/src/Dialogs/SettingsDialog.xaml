<Window x:Class="Leagueinator.GUI.Dialogs.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Leagueinator.GUI.Dialogs"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        Title="Settings" Height="246" Width="300">
    <StackPanel Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Number of Lanes" VerticalAlignment="Center" Margin="10"/>
            <TextBox x:Name="TxtLanes" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Margin="10"/>

            <Label Grid.Column="0" Grid.Row="1" Content="Default Ends" VerticalAlignment="Center" Margin="10"/>
            <TextBox x:Name="TxtEnds" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Margin="10"/>

            <ListBox x:Name="ListFormats" Width="100" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="2" HorizontalContentAlignment="Center">
                <ListBoxItem >1 vs 1</ListBoxItem>
                <ListBoxItem>2 vs 2</ListBoxItem>
                <ListBoxItem>3 vs 3</ListBoxItem>
                <ListBoxItem>4 vs 4</ListBoxItem>
                <ListBoxItem>4321</ListBoxItem>
            </ListBox>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="ButOk" Content="OK" Width="80" Margin="0,0,5,0" Click="HndOkButtonClick"/>
            <Button Content="Cancel" Width="80" Click="HndCancelButtonClick"/>
        </StackPanel>
    </StackPanel>
</Window>

